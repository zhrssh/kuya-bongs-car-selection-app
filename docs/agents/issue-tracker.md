# Issue tracker: GitHub

This project uses GitHub Issues for tracking work.

## Skills integration

- `to-issues`: Uses `gh issue create` to create new issues.
- `triage`: Reads issue states and applies labels via the GitHub API (`gh` CLI).
- `to-prd`: Creates issues linked to PRDs.
- `qa`: Files bug reports as GitHub issues.
