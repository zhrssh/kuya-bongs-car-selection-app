# Storefront Context

This context covers the React-based storefront for browsing cars.

Shared React hooks (e.g., `useDebounce`) and types/enums live in the `@repo/shared` package (`packages/shared/`). Import them from `"@repo/shared"` rather than duplicating them locally.
